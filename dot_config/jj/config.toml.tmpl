[user]
name = "Severen Redwood"
{{- if not (eq .chezmoi.hostname "yggdrasill") }}
email = "sev@severen.dev"
{{- else }}
email = "severen.redwood@sitehost.co.nz"
{{- end }}

[ui]
default-command = "log"

[git]
subprocess = true
push-bookmark-prefix = "severen/push-"

[revset-aliases]
"slice()" = "slice(@)"
# View the entire branch (and all sub-branches thereof) that the specified
# revision belongs to.
"slice(from)" = "ancestors(reachable(from, mutable()), 2)"
